import { NoteExceptionFilter } from './note-exception.filter';

describe('NoteExceptionFilter', () => {
  it('should be defined', () => {
    expect(new NoteExceptionFilter()).toBeDefined();
  });
});
